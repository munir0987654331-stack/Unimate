# UniMate

University student assistant web app — dashboard, courses, timetable, assignments,
attendance, exams, GPA/CGPA calculator, notes & resources, notifications, settings.

Frontend-only for now (mock data lives in `App.jsx` inside the `api` object —
swap those functions for real network calls once a backend exists).

## Run locally

```bash
npm install
npm run dev
